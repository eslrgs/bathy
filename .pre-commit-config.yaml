repos:
  - repo: local
    hooks:
      - id: just-format
        name: just format
        entry: just format
        language: system
        pass_filenames: false
      - id: just-typecheck
        name: just typecheck
        entry: just typecheck
        language: system
        pass_filenames: false
